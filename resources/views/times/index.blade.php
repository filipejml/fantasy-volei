<x-app-layout><x-slot name="header"><div class="flex justify-between"><h2 class="text-xl font-bold">Meus times</h2><a href="{{ route('times.create') }}" class="rounded-lg bg-blue-700 px-4 py-2 font-bold text-white">Montar time</a></div></x-slot><div class="py-8"><div class="mx-auto max-w-5xl px-4">@include('admin.partials.flash')<div class="grid gap-5 md:grid-cols-2">@forelse($times as $time)<a href="{{ route('times.edit',$time) }}" class="rounded-2xl bg-white p-6 shadow-sm ring-1 ring-slate-200"><h3 class="text-xl font-extrabold">{{ $time->nome }}</h3><p class="mt-2 text-slate-500">{{ ucfirst($time->genero) }} · {{ $time->jogadores->count() }} jogadores</p><p class="mt-4 font-bold text-blue-700">C$ {{ number_format($time->creditos_usados,2,',','.') }} / {{ number_format($time->creditos_limite,2,',','.') }}</p></a>@empty<p class="rounded-2xl bg-white p-10 text-center">Você ainda não montou um time.</p>@endforelse</div></div></div></x-app-layout>
