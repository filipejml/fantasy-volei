<x-app-layout><x-slot name="header"><div class="flex justify-between"><h2 class="text-xl font-bold">Classificação</h2><a href="{{ route('admin.classificacoes.create') }}" class="rounded-lg bg-blue-700 px-4 py-2 text-sm font-bold text-white">Nova linha</a></div></x-slot><div class="py-8"><div class="mx-auto max-w-7xl px-4">@include('admin.partials.flash')<form method="POST" action="{{ route('admin.classificacoes.calcular') }}" class="mb-5 flex flex-wrap gap-3 rounded-xl bg-white p-4">@csrf<select name="genero" class="rounded-md border-gray-300"><option value="masculino">Masculino</option><option value="feminino">Feminino</option></select><input name="temporada" type="number" value="{{ config('services.volleyball_world.season') }}" class="rounded-md border-gray-300"><button class="rounded-lg bg-slate-900 px-4 py-2 font-bold text-white">Calcular pelos resultados</button></form><div class="overflow-x-auto rounded-2xl bg-white shadow-sm"><table class="min-w-full divide-y"><thead class="bg-slate-50"><tr>@foreach(['#','Seleção','Gênero','J','V','D','Pts','Origem',''] as $h)<th class="px-4 py-3 text-left text-xs font-bold uppercase text-slate-500">{{ $h }}</th>@endforeach</tr></thead><tbody class="divide-y">@forelse($classificacoes as $linha)<tr><td class="px-4 py-3 font-bold">{{ $linha->posicao }}</td><td class="px-4 py-3 font-bold">{{ $linha->selecao->nome }}</td><td class="px-4 py-3">{{ ucfirst($linha->genero) }}</td><td class="px-4 py-3">{{ $linha->jogos }}</td><td class="px-4 py-3">{{ $linha->vitorias }}</td><td class="px-4 py-3">{{ $linha->derrotas }}</td><td class="px-4 py-3 font-bold">{{ $linha->pontos }}</td><td class="px-4 py-3">{{ ucfirst($linha->origem) }}</td><td class="px-4 py-3"><a class="font-bold text-blue-700" href="{{ route('admin.classificacoes.edit',$linha) }}">Editar</a></td></tr>@empty<tr><td colspan="9" class="p-10 text-center">Sem classificação.</td></tr>@endforelse</tbody></table><div class="p-4">{{ $classificacoes->links() }}</div></div></div></div></x-app-layout>
